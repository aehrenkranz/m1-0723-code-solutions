/* exported setValue */
/**
 * define a function named setValue with 3 parameters:object, 'key' 'and' 'value'
 * * assign the value of the 'key' variable to the 'object' variable as a property, and give that property the value of the 'value' variable
 * * do not return anything
 */
function setValue(object, key, value) {
  object[key] = value;
}
